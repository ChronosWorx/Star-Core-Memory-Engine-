# Star-Core-Memory-Engine-
​A drop-in Kotlin memory engine for local LLMs. Uses deterministic hierarchical indexing (STAR) to decouple persistent knowledge storage from the active context window, enabling infinite-horizon memory on edge devices.
